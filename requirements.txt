# Full local environment for the Huronalytics pipeline + tools.
# CI installs only the pipeline subset inline (see
# .github/workflows/update-leaderboard.yml): gspread, google-auth, numpy,
# pandas, scikit-learn, xgboost.

# Pipeline (pipeline/process_data.py + Stuff+ trainer)
gspread>=6.0,<7.0
google-auth>=2.0,<3.0
numpy
pandas
scikit-learn
xgboost==3.3.0

# Cards + report rendering (cards/pitcher.py, cards/hitter.py)
matplotlib
Pillow

# Scrapers + Savant/MLB API pulls (scrapers/pitcher2026.py,
# scrapers/mlb_transactions.py, pipeline/guts.py)
requests
beautifulsoup4
openpyxl
