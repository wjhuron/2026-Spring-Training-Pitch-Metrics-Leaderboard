# Full local environment for the Huronalytics pipeline + tools.
# CI installs only the pipeline subset inline (see
# .github/workflows/update-leaderboard.yml): gspread, google-auth, numpy,
# pandas, scikit-learn, xgboost.

# Pipeline (process_data.py + Stuff+ trainer)
gspread>=6.0,<7.0
google-auth>=2.0,<3.0
numpy
pandas
scikit-learn
xgboost==3.3.0

# Cards + report rendering (Cards.py, HitterCards.py)
matplotlib
Pillow

# Scrapers + Savant/MLB API pulls (Pitcher2026.py, MLBScraper.py, guts.py)
requests
beautifulsoup4
openpyxl

# Dormant Supabase mirror (supabase_append.py; default off since 2026-06-23)
psycopg2-binary
