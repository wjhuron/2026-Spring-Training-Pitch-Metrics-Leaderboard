# Secrets
service_account.json
.env
.env.*
!.env.example

# Local data / caches
*.xlsx
*.pkl
mlb_id_cache.json
outlier_results.json
name_duplicates_report.txt
data/fg_aaa_wrcplus.json
data/fg_overrides.json
# release asset, rebuilt each CI run
data/fg_overrides.json.enc
# micro_data is redundant in-repo: the browser loads microData from
# data_embedded.json.gz; the raw file is a local-only pipeline artifact
# (fallback for local dev + diagnostic scripts). ~51 MB, was tripping
# GitHub's large-file warning on every data commit.
data/micro_data*.json
# per-pitch grade dumps for the Sheets write-back (derived, local-only)
data/pitch_loc_grades_*.json
data/pitch_stuff_grades*.json
# ABS full pitch-level dataset (~65 MB; the site reads the smaller
# abs_challenge_events file, this one is a local pipeline input only)
data/abs_challenges_2026.json
# local lookup caches + experiment outputs (guard against `git add data/`)
data/hitter_bats_cache.json
data/_pplus_*.csv

# Python
.venv/
venv/
__pycache__/
*.pyc

# OS
.DS_Store

# IDE
.idea/
.vscode/

# Claude
.claude/

# Analysis script artifacts
scripts/cache/
scripts/outputs/

# Biomech: keep the .py scripts but never the heavy assets/outputs
*.task

# Stuff+ v10 model artifacts (only the training script is in git;
# v10 archived 2026-07-29)
archive/stuff_plus_v10/pitcher_stuff_v10.csv
archive/stuff_plus_v10/model_summary_v10.txt

# Logs
*.log

# encrypted transport artifacts
*.pkl.enc
scripts/_feed_platexz_cache.pkl
scripts/_feed_meta_cache.pkl
artifacts/
